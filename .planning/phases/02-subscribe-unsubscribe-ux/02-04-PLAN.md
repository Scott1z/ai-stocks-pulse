---
phase: 02-subscribe-unsubscribe-ux
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - .planning/phases/02-subscribe-unsubscribe-ux/02-04-SUMMARY.md
autonomous: true
requirements: [OPTIN-01, OPTIN-02, OPTIN-03, OPTIN-04, OPTIN-05, OPTIN-06]

must_haves:
  truths:
    - "The deployed site serves index.html unchanged — adding package.json did not turn the project into a build-step deployment"
    - "The deployed /api/subscribe and /api/unsubscribe endpoints respond correctly to method and validation probes"
    - "A real browser subscription made through the UI appears in the push:subscriptions Redis hash"
    - "Unsubscribing through the UI removes that row from Redis, verified with the Phase 1 CLI"
    - "All five ROADMAP Phase 2 success criteria are confirmed against the deployed site"
  artifacts:
    - path: ".planning/phases/02-subscribe-unsubscribe-ux/02-04-SUMMARY.md"
      provides: "Recorded evidence for each of the six OPTIN requirements and the deployed production URL"
  key_links:
    - from: "deployed /api/subscribe"
      to: "push:subscriptions Redis hash"
      via: "a real browser subscription round trip, confirmed with scripts/push_redis.py get"
      pattern: "push:subscriptions"
---

<objective>
Prove the whole phase works on the deployed site, end to end, and capture the evidence.

This plan writes no product code. Everything before it was verified in isolation: plan `02-01`'s Functions against Redis via a Node harness, `02-02`'s shell by inspection, `02-03`'s logic by syntax and source gates. None of that exercised the real path — **`/api/*` does not run under `python3 -m http.server`, so the subscribe/unsubscribe round trip is only real on Vercel.** This plan closes that gap and answers the one deployment risk this phase introduced: does the repo's first `package.json` change how Vercel builds and serves the static site?

Purpose: satisfy the five ROADMAP Phase 2 success criteria and produce the verification record `/gsd:verify-work` will read.
Output: `.planning/phases/02-subscribe-unsubscribe-ux/02-04-SUMMARY.md` with per-requirement evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-subscribe-unsubscribe-ux/02-UI-SPEC.md
@.planning/phases/02-subscribe-unsubscribe-ux/02-01-SUMMARY.md
@.planning/phases/02-subscribe-unsubscribe-ux/02-02-SUMMARY.md
@.planning/phases/02-subscribe-unsubscribe-ux/02-03-SUMMARY.md

<interfaces>
<!-- Phase 1 verification CLI — no new tooling needed for the Redis half. -->
python3 scripts/push_redis.py list             # all stored subscriptions
python3 scripts/push_redis.py get <endpoint>   # one subscription, by raw endpoint
python3 scripts/push_redis.py del <endpoint>   # manual cleanup

<!-- Requires .env.local sourced first (KV_REST_API_URL / KV_REST_API_TOKEN): -->
set -a; . ./.env.local; set +a

<!-- The five ROADMAP Phase 2 success criteria being verified: -->
1. Visitors see an in-app soft-ask driven by their own action/context — never automatically on first page load.
2. The native browser permission prompt appears only after a visitor explicitly accepts the soft-ask.
3. iOS visitors who have not added the app to their home screen see an honest "install first" message
   instead of a non-functional enable button.
4. Subscribed visitors see a persistent toggle reflecting live subscription state, and toggling off
   deletes the subscription from Redis (server-side), not just locally.
5. Visitors who have denied browser notification permission see a quiet help message and are never
   shown the soft-ask or native prompt again.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-file consistency gates before deploying</name>
  <files>(read-only — no files modified)</files>
  <read_first>
    - `.planning/phases/02-subscribe-unsubscribe-ux/02-02-SUMMARY.md` — the element-id contract `app.js` must match
    - `.planning/phases/02-subscribe-unsubscribe-ux/02-03-SUMMARY.md` — the state list and the two sanctioned `requestPermission()` call sites
    - `index.html`, `app.js`, `styles.css`, `service-worker.js` — the four files this phase changed on the frontend
    - `package.json` — confirm no `build` script slipped in
  </read_first>
  <action>
    Run the consistency checks that no individual plan could run, because each one only owned part of the surface. These are cheap source assertions, executed before spending a deploy cycle:

    1. Every element id `app.js` looks up must exist in `index.html`, and every id `index.html` defines for this feature must be referenced by `app.js` — a silent typo here produces a feature that does nothing with no error.
    2. Every CSS class `index.html` uses on the new elements must have a matching rule in `styles.css`.
    3. `Notification.requestPermission()` appears exactly twice in non-comment `app.js` source.
    4. `CACHE_NAME` is `ai-stocks-pulse-v48` and no stale `v47` remains.
    5. `package.json` has no `build` script and `node_modules` is git-ignored while `package-lock.json` is not.
    6. The working tree is clean apart from expected files, and `git status --porcelain` never lists `node_modules`.

    If any gate fails, fix it in the owning file before continuing — do not deploy a known-broken wiring.
  </action>
  <verify>
    <automated>python3 -c "
import re,subprocess
h=open('index.html',encoding='utf-8').read()
a=''.join(l for l in open('app.js',encoding='utf-8') if not l.lstrip().startswith('//'))
c=open('styles.css',encoding='utf-8').read()
sw=open('service-worker.js',encoding='utf-8').read()
ids=['pushSoftAsk','pushSoftAskText','pushSoftAskAccept','pushSoftAskClose','pushIosNote','pushToggle']
for i in ids:
    assert 'id=\"%s\"'%i in h, 'index.html missing id '+i
    assert '\"%s\"'%i in a, 'app.js never references id '+i
for cls in ['push-soft-ask','push-soft-ask-close','icon-bell-outline','icon-bell-filled','icon-bell-slash']:
    assert cls in h, 'index.html missing class '+cls
for cls in ['.push-soft-ask','.push-soft-ask-close','.icon-bell-outline','.icon-bell-filled','.icon-bell-slash']:
    assert cls in c, 'styles.css missing rule for '+cls
assert a.count('Notification.requestPermission()')==2, 'expected exactly 2 requestPermission call sites'
assert 'ai-stocks-pulse-v48' in sw and 'v47' not in sw, 'CACHE_NAME must be v48'
import json; p=json.load(open('package.json'))
assert not (p.get('scripts') or {}).get('build'), 'package.json must not have a build script'
st=subprocess.run(['git','status','--porcelain'],capture_output=True,text=True).stdout
assert 'node_modules' not in st, 'node_modules must be git-ignored'
print('cross-file gates OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - All six element ids exist in `index.html` AND are referenced by name in `app.js`
    - All five new class names appear in both `index.html` and `styles.css`
    - Non-comment `app.js` contains exactly two `Notification.requestPermission()` call sites
    - `service-worker.js` `CACHE_NAME` is `ai-stocks-pulse-v48` with no `v47` remnant
    - `package.json` has no `scripts.build`
    - `git status --porcelain` output contains no `node_modules` entry
  </acceptance_criteria>
  <done>Wiring across the four frontend files and the package manifest is internally consistent; safe to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy and probe the live endpoints</name>
  <files>(no files modified — deploy and probe only)</files>
  <read_first>
    - `DEPLOY.md` — how this project reaches Vercel (git push to `origin/main` triggers the deploy; there is no Vercel CLI installed)
    - `.vercel/project.json` — the project name (`ai-stocks-pulse`) used to resolve the production URL
    - `.planning/phases/02-subscribe-unsubscribe-ux/02-01-SUMMARY.md` — the exact request/response contract of both Functions
    - `api/subscribe.js`, `api/unsubscribe.js` — the status codes to expect from each probe
  </read_first>
  <action>
    Commit and push all of this phase's work to `origin/main` so Vercel builds and deploys it. `branching_strategy` is `none` for this project, so `main` is the deploy branch. Do not push `node_modules`; DO push `package.json` and `package-lock.json` — Vercel needs the lockfile to install `@upstash/redis` for the Functions.

    Resolve the production URL. `.vercel/project.json` records `projectName: ai-stocks-pulse` but not the domain; the site is almost certainly at `https://ai-stocks-pulse.vercel.app`. Confirm it responds before probing, and if it does not, get the real domain from the Vercel dashboard (Project → Domains). Record whichever URL you used in the summary — this phase is the first time the repo needs it written down, and Phase 4 will need it for the pipeline trigger.

    Then probe the deployment with `curl` against that base URL:

    1. **Static site still served.** `GET /` returns 200 and its body contains `AI QuickCap` and `id="pushToggle"`. This is the gate that answers the `package.json` risk: if Vercel had switched to build-step mode, this would fail or serve something unexpected.
    2. **`/api/subscribe` rejects GET** → 405.
    3. **`/api/subscribe` rejects a malformed body** (`{"subscription":{"endpoint":"x"}}`, `Content-Type: application/json`) → 400.
    4. **`/api/subscribe` rejects a non-https endpoint** (`http://example.com/x` with valid-looking keys) → 400.
    5. **`/api/unsubscribe` rejects GET** → 405.
    6. **`/api/unsubscribe` with an unknown endpoint** (`{"endpoint":"https://fcm.googleapis.com/fcm/send/aisp-probe-<random>"}`) → 200. This proves the idempotent-delete contract holds in the deployed runtime, which is what Phase 3's pruning will depend on.
    7. **Cold-start sanity.** Confirm none of the above returned a 500 — a 500 on any probe means `Redis.fromEnv()` failed to find `KV_REST_API_URL`/`KV_REST_API_TOKEN` in the Vercel environment, or the ESM `import` did not resolve. Both are configuration failures that must be fixed before the browser test in Task 3.

    Do NOT send a real, valid subscription through curl — a hand-forged subscription would land a permanently undeliverable row in `push:subscriptions`. Valid-path coverage comes from the real browser subscription in Task 3.

    After the probes, confirm the hash is unpolluted: `python3 scripts/push_redis.py list` shows no `aisp-probe-` or `aisp-selftest-` endpoints.
  </action>
  <verify>
    <automated>BASE="${PUSH_BASE_URL:-https://ai-stocks-pulse.vercel.app}"; set -e; code(){ curl -s -o /dev/null -w '%{http_code}' "$@"; }; test "$(code "$BASE/")" = 200 && curl -s "$BASE/" | grep -q 'id="pushToggle"' && test "$(code -X GET "$BASE/api/subscribe")" = 405 && test "$(code -X POST -H 'Content-Type: application/json' -d '{"subscription":{"endpoint":"x"}}' "$BASE/api/subscribe")" = 400 && test "$(code -X POST -H 'Content-Type: application/json' -d '{"subscription":{"endpoint":"http://example.com/x","keys":{"p256dh":"aaa","auth":"bbb"}}}' "$BASE/api/subscribe")" = 400 && test "$(code -X GET "$BASE/api/unsubscribe")" = 405 && test "$(code -X POST -H 'Content-Type: application/json' -d "{\"endpoint\":\"https://fcm.googleapis.com/fcm/send/aisp-probe-$RANDOM\"}" "$BASE/api/unsubscribe")" = 200 && (set -a; . ./.env.local; set +a; python3 scripts/push_redis.py list | grep -c -e 'aisp-probe-' -e 'aisp-selftest-' | grep -qx 0) && echo "live probes OK against $BASE"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /` on the deployed URL returns 200 and its body contains `id="pushToggle"` — the static site is still served directly, and the new shell is live
    - `GET /api/subscribe` and `GET /api/unsubscribe` each return 405
    - `POST /api/subscribe` with a body missing `keys` returns 400
    - `POST /api/subscribe` with an `http://` endpoint returns 400
    - `POST /api/unsubscribe` with a never-stored endpoint returns 200 (idempotent contract confirmed in the real Vercel runtime)
    - No probe returns 500 — `Redis.fromEnv()` resolved the Vercel environment variables and the ESM import loaded
    - `python3 scripts/push_redis.py list` shows zero `aisp-probe-` and zero `aisp-selftest-` rows
    - The production base URL actually used is recorded verbatim in `02-04-SUMMARY.md`
  </acceptance_criteria>
  <done>The deployment serves the static site unchanged and both Functions behave correctly in Vercel's real runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all six OPTIN behaviors against the deployed site</name>
  <files>(no files modified — evidence goes into the plan summary)</files>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 2's five success criteria, which these checks map onto
    - `.planning/REQUIREMENTS.md` — OPTIN-01..06 exact wording
    - `.planning/phases/02-subscribe-unsubscribe-ux/02-UI-SPEC.md` — the Copywriting Contract, so the copy on screen can be compared against the locked strings
    - `.planning/phases/02-subscribe-unsubscribe-ux/02-03-SUMMARY.md` — the state machine, so an unexpected toggle state can be traced to the right branch
  </read_first>
  <action>
    Run the full behavioral pass against the deployed URL from Task 2 and record the outcome of each check in `02-04-SUMMARY.md`, mapped to its requirement ID. `human_verify_mode` is `end-of-phase` for this project, so these are declared as `<human-check>` items and collected at the phase gate rather than pausing execution here.

    The Redis half of check D is automatable and must be run as such: capture the subscription endpoint from the DevTools console after subscribing, then confirm presence and later absence with `python3 scripts/push_redis.py get <endpoint>`. That command is the objective evidence for OPTIN-05 — do not accept "the toast said it worked" as proof, since a client-only unsubscribe would produce exactly the same toast.

    Cleanup: after the pass, ensure the test browser's subscription is gone from Redis. If the unsubscribe check left a row behind (which would itself be a failure), remove it with `python3 scripts/push_redis.py del <endpoint>` and record the discrepancy rather than hiding it.

    A failure in any check is a real gap — record it in the summary as a gap with the requirement ID attached, so `/gsd:verify-work` can route it to `--gaps` planning. Do not paper over a partial pass.
  </action>
  <verify>
    <automated>set -a; . ./.env.local; set +a; python3 scripts/push_redis.py list | grep -c -e 'aisp-probe-' -e 'aisp-selftest-' | grep -qx 0 && echo "redis clean after behavioral pass"</automated>
    <human-check>
**Verify against the deployed URL recorded in Task 2. Use a fresh browser profile (or clear this site's data + notification permission) before starting.**

**A — OPTIN-01: the soft-ask is never on page load.**
1. Open the site in a fresh profile. At the moment of first paint, and for the next ~5 seconds: no banner, no bell icon in the topbar, no native prompt.
2. Around 6 seconds in, the banner slides up from the bottom of the screen with the text "Recibí el resumen del sector todos los días al cierre del mercado, sin abrir la app.", an "Activar" button, and an × .
3. While the banner is up, scroll the page and click a stock row — the page is fully interactive behind it (non-blocking, per D-02).
Expected: no notification UI at t=0; banner at t≈6s; page never blocked.

**B — OPTIN-02: the native prompt only fires after an explicit accept.**
1. With the banner up, confirm no native permission prompt has appeared on its own.
2. Click "Activar". The native browser permission prompt appears now, and only now.
3. Accept it. A toast reads "¡Notificaciones activadas!" and the topbar bell turns solid accent-colored.
Expected: native prompt strictly after the click.

**C — OPTIN-04: the toggle reflects live state.**
1. Reload the page while subscribed. The bell renders in its filled/accent subscribed state on load, without any banner.
2. Hover it: the tooltip/aria-label reads "Notificaciones activadas — click para desactivar".
Expected: state survives reload because it is derived from the real subscription, not a local flag.

**D — OPTIN-05: unsubscribe deletes the row server-side.**
1. Before unsubscribing, in DevTools console run: `(await (await navigator.serviceWorker.ready).pushManager.getSubscription()).endpoint` and copy the endpoint string.
2. In a terminal: `set -a; . ./.env.local; set +a; python3 scripts/push_redis.py get "<endpoint>"` → prints the stored subscription JSON. **Record this output.**
3. Click the topbar bell once. Toast reads "Notificaciones desactivadas." with NO confirmation dialog, and the bell returns to its outline/unsubscribed state.
4. Re-run the same `push_redis.py get "<endpoint>"` command → prints nothing. **Record this output.**
Expected: present before, absent after. This is the objective proof for OPTIN-05.

**E — OPTIN-06: denied is quiet and permanent.**
1. In browser site settings, set Notifications to Block for this site. Reload.
2. No banner appears — not at t=0, not at t=6s, not ever.
3. The bell renders faint grey (not red) with a slash.
4. Click it: a toast reads "Bloqueaste las notificaciones en tu navegador. Para activarlas, entrá a la configuración del sitio (ícono de candado en la barra de direcciones) y permitilas." and **no native prompt appears**.
Expected: quiet help only; the browser is never re-asked.

**F — OPTIN-03: the iOS install-first message (real iPhone required).**
1. On a real iPhone, open the site in Safari as a normal tab (NOT from a home-screen icon).
2. Wait ~6 seconds: the banner appears showing "Las notificaciones en iPhone requieren instalar la app primero. Tocá compartir → "Agregar a inicio", abrí AI QuickCap desde el ícono nuevo y activalas desde ahí."
3. There is NO "Activar" button in the banner and NO bell icon in the topbar — nothing tappable that could silently fail.
4. Add the app to the home screen, open it from the new icon, and confirm the bell now appears and the normal subscribe flow (checks A–D) works there.
Expected: honest message in the tab, functional control only in standalone mode. A DevTools UA override is not acceptable evidence for this check — `navigator.standalone` is unreliable to simulate; use a real device.

**G — dismissal never nags.**
1. Fresh profile, wait for the banner, click × .
2. Reload the page several times and wait past 6 seconds each time: the banner never returns.
3. Click the topbar bell: the flow still opens (this is the only path back, by design).
Expected: dismissal is permanent for the automatic path; the deliberate toggle still works.

**H — dark mode and mobile layout.**
1. Toggle dark mode: the banner and all three bell states render correctly with no hardcoded light-mode color.
2. Narrow the window below 640px: the banner spans full width with side insets, wraps cleanly, and never causes horizontal scroll.

Report any check that fails with its letter and the requirement ID.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `02-04-SUMMARY.md` records a PASS/FAIL line for each of checks A–H, each annotated with the requirement IDs it covers (A→OPTIN-01, B→OPTIN-02, C→OPTIN-04, D→OPTIN-05, E→OPTIN-06, F→OPTIN-03, G→OPTIN-01, H→UI-SPEC conformance)
    - Check D's evidence includes the literal terminal output of `push_redis.py get <endpoint>` both before (non-empty) and after (empty) the unsubscribe — not a paraphrase
    - Check E confirms no native prompt appeared, which is the OPTIN-06 guarantee
    - Check F is recorded as performed on a real iPhone, or explicitly recorded as BLOCKED with the reason if no device was available — never silently marked PASS from a UA override
    - `python3 scripts/push_redis.py list` at the end shows no leftover test rows
    - Any failing check is written up as a gap with its requirement ID rather than omitted
  </acceptance_criteria>
  <done>All six OPTIN requirements have recorded evidence against the deployed site, with the OPTIN-05 server-side deletion proven by the Phase 1 Redis CLI rather than by UI feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → `origin/main` → Vercel production | This plan's push deploys to the live site; a mistake here is publicly visible |
| curl probes → deployed `/api/*` | Verification traffic crosses the same untrusted boundary a real attacker would |
| test browser subscription → `push:subscriptions` | A real, deliverable subscription is created during the pass and must be removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | `push:subscriptions` pollution from verification | mitigate | Probes use only invalid or clearly-marked `aisp-probe-` endpoints; a Redis-clean assertion runs in both Task 2 and Task 3; the real browser subscription is removed by check D itself and re-verified at the end |
| T-02-15 | Information Disclosure | endpoint captured during check D | mitigate | The endpoint is used only in a local terminal command and must NOT be pasted into `02-04-SUMMARY.md` — record the *shape* of the output (present/absent), redacting the endpoint itself, since it is per-device key material |
| T-02-16 | Denial of Service | deploying a broken shell to production | mitigate | Task 1's cross-file gates run before the push, and Task 2's first probe asserts `GET /` still serves the real page before any behavioral work begins |
| T-02-17 | Elevation of Privilege | Vercel build-mode change from the new `package.json` | mitigate | Task 1 asserts no `build` script; Task 2 probe 1 asserts the deployed `/` still contains the expected markup |
</threat_model>

<verification>
1. Task 1's cross-file gate script exits 0.
2. Task 2's curl probe chain exits 0 against the resolved production URL, with no 500 anywhere.
3. Check D's before/after `push_redis.py get` outputs demonstrate the row was created and then deleted.
4. `python3 scripts/push_redis.py list` is free of `aisp-probe-`, `aisp-selftest-`, and the test browser's subscription at the end of the pass.
5. All five ROADMAP Phase 2 success criteria have a corresponding recorded check.
</verification>

<success_criteria>
- The deployed site serves the static page unchanged — the repo's first `package.json` did not alter Vercel's serving behavior.
- Both Functions behave correctly in Vercel's real runtime, including the idempotent-unsubscribe contract Phase 3 depends on.
- A real browser subscription round-trips into Redis and is provably removed by a single toggle click, evidenced by `scripts/push_redis.py`.
- Every one of OPTIN-01..06 has a recorded PASS, or a recorded gap with its requirement ID.
- The production base URL is written down for Phase 4's pipeline trigger.
</success_criteria>

<output>
Create `.planning/phases/02-subscribe-unsubscribe-ux/02-04-SUMMARY.md` when done.

The summary must contain: the production base URL used, the per-requirement PASS/FAIL table for checks A–H, the redacted before/after evidence for OPTIN-05, and any gap written up with its requirement ID so `/gsd:verify-work` can route it. Do not paste a real push `endpoint` into the summary — it is per-device key material.
</output>
